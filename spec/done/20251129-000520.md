# TODO 2025-11-29 00:05:20

- [x] CLI で `--subscription-id/-s` を指定した際に、`c7n.utils.local_session` のキャッシュをリセットし、`c7n_azure.session.Session(subscription_id=<指定値>)` で再初期化する仕組みを実装する。
- [x] 上記挙動をユニットテストで検証し、`AZURE_SUBSCRIPTION_ID` が常に明示指定の値へ更新されること、ならびに既定サブスクリプションへフォールバックしないことを保証する。
- [x] README など利用手順に、明示指定時はセッションキャッシュを再初期化する旨を補足する（必要に応じて）。
