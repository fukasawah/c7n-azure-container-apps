# How to Write Tests (tests/)

`tests/` 配下のテストは、pytest を用いて TDD を実践するための土台とする。

- Happy path + 代表的なエラーケースを必ず含める。
- テスト名・テストケースは、仕様（SPEC）上の要件と対応づけて分かるようにする。
- 意味のあるアサーションを行う。nullでないこと、空でないこと、を確認するだけのアサーションは行わない。
